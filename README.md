# Sistema de organização de defesas - Frontend

Para instalar os pacotes necessários, digite o comando:

``` npm install ```

Para rodar o sistema, digite o comando:

``` npm start ```

Para fazer deploy:

``` npm run deploy ```
